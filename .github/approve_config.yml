approvals:
  # check will fail if there are not at least 4 approvals in total
  minimum: 4
  groups:
    # check will fail if there is not at least 1 approval from backend
    backend:
      minimum: 1
      from:
        users:
          - yuichielectric
          - dchomh
    # check will fail if there are not at least 2 approvals from frontend
    frontend:
      minimum: 2
      from:
        users:
          - dchomh
          - rerwinx
